[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "sharp123"
version = "0.0.1"
dependencies = [
    "build123d >= 0.11.1",
    "ocp_vscode >= 4.0.1",
]